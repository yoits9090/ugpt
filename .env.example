# Next.js app
# Optional for local dev. The API route already falls back to http://localhost:3001.
BACKEND_API_URL=http://localhost:3001

# Optional public fallback if you want the same value exposed to the client.
NEXT_PUBLIC_API_URL=
